name: 极简测试工作流

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: 测试打印
        run: |
          echo "你好，GitHub Actions！如果你能看到这个，说明工作流已经成功了！"
          date